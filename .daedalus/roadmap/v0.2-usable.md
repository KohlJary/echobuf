# v0.2 — Usable

**Status:** backlog
**Priority:** P1
**Type:** milestone

## Scope

Config file, output templates, IPC socket, CLI client commands, systemd unit.

## Work Items

1. Config file parsing (TOML)
2. Output template parser (yt-dlp style)
3. IPC via Unix domain socket (line-delimited JSON)
4. CLI client commands (save, status, sources, quit)
5. systemd user unit file
